import React, { Component } from 'react'

export default class Home extends Component {
  render() {
    return (
      <div className="container">
        <h4>This is an React app that fetch data from API, I´m using context API, list user where you can search users and open user view with details on user location using google API.</h4>
      </div>
    )
  }
}
