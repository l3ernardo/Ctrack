import React, { Component } from 'react'

export default class Home extends Component {
  render() {
    return (
      <div className="container">
        <h4>This is an React app that fetch´s data using context API, list group user´s and use google API key for user lacation</h4>
        <h4>CSS is pure testing, creating stuff out the box, the idea is not use boostrap or material ui etc... this is for practice.</h4>
      </div>
    )
  }
}
