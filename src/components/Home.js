import React, { Component } from 'react'

export default class Home extends Component {
  render() {
    return (
      <div className="container">
        <p>We would like you to build a simple webpage that shows / lists the current Team users.</p>

        <p>You should use the following endpoint to fake data.</p>

        <p>Provide users the ability to search by name or email (and other attributes, optional).</p>

        <p>Provide an easy way so that users can contact a Team member.</p>

        <p>A user should be able to navigate the interface via keyboard.</p>

        <p>The app will be responsive and render well on modern desktop browsers as well as mobile devices.</p>
                
      </div>
    )
  }
}
